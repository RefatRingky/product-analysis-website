import React from 'react';

const Blog = () => {
    return (
        <div>
            <h1> Question Answer</h1>
            <h2>1.What is context API?</h2>
            
            
            <p>1.Those component have common functionality this type of component can use context API for reduce the duplicate code and lack of re-usability.</p>
            <p>2.Context API is a way for react app to effectively produced a global variable that can be passed around.</p>
            <p>3.Context API is the alternative to the prop-drilling method.</p>
            <p>4.If we create a context API  at first we need to create   React.createContext().</p>
           <p> 5.Providor is a component that provide a state to its children and it will hold the store and be the parent of all the components that might need that store.</p>
            <p>6.On the otherhand consumer that consumes  the component and use the state.</p>
            
            <h2>2. What is Semantic Tag?</h2>
            <p>
            <p>1.Semantic tag means those tag are describe the meaning human and machine readable way for example. etc.</p>
            <p> 2.Semantic tag is one of the most important features of HTML5 ,its refer a syntax which is more comprehensible.</p>
            <p>3.It makes a web pages more informative and  adaptable.</p>
            <p> 4.Define the purpose of any element we can use semantic tag.</p>
            <p> 5. By using semantic tag, we can help the browser to understand the meaning of the content  better way.</p>
            <p>6.Provide extra level of clarity,it also help to search engines to read the page and find the requirement more faster.</p>
            
           
            
           
           
            

            </p>
        </div>
    );
};

export default Blog;